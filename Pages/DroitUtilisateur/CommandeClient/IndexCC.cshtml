@page
@model WebApplication.Pages.DroitUtilisateur.CommandeClient.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="CreateCC">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CmdClient[0].NumCommande)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CmdClient[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CmdClient[0].Heure)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CmdClient[0].Prix)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CmdClient) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumCommande)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Heure)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prix)
            </td>
            <td>
                <a asp-page="./EditCC" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./DetailsCC" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./DeleteCC" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
